export interface Work {
  className?: string
  author: string,
  title: string,
  publisher: string,
  mounth?: string,
  fascicle?: number,
  volume?: string,
  initialPage?: string,
  finalPage?: string,
  year: string,
  locale: string,
}